# Frontend build arg (baked into Next.js at image build time)
# Production: cp .env.compose.example .env.compose in ~/production/trading-journal/
# Local dev:  cp .env.compose.example .env.compose in repo root
NEXT_PUBLIC_API_BASE_URL=https://api.trade2learn.xyz/
