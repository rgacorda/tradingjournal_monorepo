# Production: copy to ~/production/.env (compose project directory)
# Local dev: copy to repo root as .env
# cp .env.compose.example ~/production/.env
NEXT_PUBLIC_API_BASE_URL=https://api.trade2learn.site/
